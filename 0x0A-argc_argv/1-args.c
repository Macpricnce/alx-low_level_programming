#include "main.h"
/**
 * main - the entry point of the program
 * @argc: the arg count
 * @argv: the arr of arg
 * Return: always 0
 */
int main(int argc, char *argv[] __attribute__ ((unused)))
{
	printf("%d\n", argc - 1);
	return (0);
}
